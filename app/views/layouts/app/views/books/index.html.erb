<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cyberpunk & Sci-Fi Book List</title>
    <style>
        body {
            font-family: 'Courier New', monospace;
            background-color: #121212;
            color: #e0e0e0;
            margin: 0;
            padding: 0;
        }
        header {
            background-color: #1f1f1f;
            padding: 1rem;
            text-align: center;
        }
        table {
            width: 90%;
            margin: 2rem auto;
            border-collapse: collapse;
        }
        th, td {
            padding: 1rem;
            text-align: left;
            border-bottom: 1px solid #444;
        }
        th {
            background-color: #333;
            color: #ddd;
        }
        tr:hover {
            background-color: #222;
        }
        a {
            color: #4caf50;
            text-decoration: none;
        }
        a:hover {
            text-decoration: underline;
        }
        .actions {
            text-align: center;
        }
        .new-book {
            display: block;
            width: 200px;
            margin: 1rem auto;
            padding: 1rem;
            background-color: #4caf50;
            color: white;
            text-align: center;
            border-radius: 5px;
        }
        footer {
            text-align: center;
            padding: 1rem;
            background-color: #1f1f1f;
            position: fixed;
            bottom: 0;
            width: 100%;
        }
    </style>
</head>
<body>

<header>
    <h1>Cyberpunk & Sci-Fi Book List</h1>
</header>

<div>
    <%= link_to 'Add New Book', new_book_path, class: 'new-book' %>
</div>

<table>
    <tr>
        <th>Title</th>
        <th>Author</th>
        <th>Description</th>
        <th>Actions</th>
    </tr>
    <% @books.each do |book| %>
        <tr>
            <td><%= book[:title] %></td>
            <td><%= book[:author] %></td>
            <td><%= book[:description] %></td>
            <td class="actions">
                <%= link_to 'Edit', edit_book_path(book[:id]) %> |
                <%= link_to 'Delete', book, method: :delete, data: { confirm: 'Are you sure?' } %>
            </td>
        </tr>
    <% end %>
</table>

<footer>
    <p>&copy; 2024 CyberBookNook - Coffee, Code & Literature</p>
</footer>

</body>
</html>
